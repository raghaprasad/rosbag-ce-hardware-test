# ROSBag Uploader Node - Perf tests

End-to-End test of ROSBag uploader node functionality. 
This integration test package interacts with actual AWS services and is designed to run on an actual robot.

### Run tests
* `$ roslaunch rosbag_ce_hardware_tests_ros1 run_tests.launch`
